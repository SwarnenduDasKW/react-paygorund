// watcher saga listens to an action and invokes worker saga
// takeEvery is a non blocking effect but take is a blocking effect.
//

import imagesSaga from "./imagesSaga";

export default imagesSaga;
